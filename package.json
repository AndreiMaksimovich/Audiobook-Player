{
  "name": "workspace",
  "version": "1.0.0",
  "private": "true",
  "scripts": {
    "start-server": "npm run dev --prefix apps/audiobook-player-server",
    "start-client": "npm run start --prefix apps/audiobook-player-rn-app",
    "start-client-https-proxy": "npx local-ssl-proxy --source 443 --target 8081 --cert secrets/localhost.crt --key secrets/localhost.key"
  },
  "workspaces": [
    "apps/*",
    "packages/*",
    "tools/build-export",
    "tools/build-import",
    "tools/build-import-docker",
    "tools/build-mobile"
  ],
  "devDependencies": {

  },
  "dependencies": {

  }
}
